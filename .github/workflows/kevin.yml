name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: Perancangan Gameplay
    - name: Run a multi-line script
      run: |
        echo Membuat gameplay utama untuk level 1
        echo On proses gan
